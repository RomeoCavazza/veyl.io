name: CI/CD Pipeline

permissions:
  contents: read
  deployments: write
  actions: read

on:
  push:
    branches: [main]
    # Only deploy on main branch, and only if source code changes
    # Using paths (not paths-ignore) to explicitly include only relevant files
    paths:
      - 'apps/frontend/**'
      - 'apps/backend/**'
      - '.github/workflows/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'requirements.txt'
      - 'Dockerfile'
      - 'start.sh'
  pull_request:
    branches: [main]
    paths:
      - 'apps/frontend/**'
      - 'apps/backend/**'
  # Allow manual deployment via workflow_dispatch
  workflow_dispatch:

jobs:
  # Check if deployment is needed
  check-changes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v2
        id: filter
        with:
          filters: |
            frontend:
              - 'apps/frontend/**'
            backend:
              - 'apps/backend/**'
              - 'start.sh'
              - 'Dockerfile'
              - 'requirements.txt'

  frontend:
    runs-on: ubuntu-latest
    needs: check-changes
    if: needs.check-changes.steps.filter.outputs.frontend == 'true' || github.event_name == 'workflow_dispatch'
    outputs:
      vercel-deploy-outcome: ${{ steps.vercel-deploy.outcome }}
    defaults:
      run:
        working-directory: ./apps/frontend
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
          
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        
      - name: Type check
        run: pnpm type-check
        
      - name: Build
        run: pnpm build
        id: build
        
      - name: Deploy to Vercel (production only)
        if: github.ref == 'refs/heads/main'
        id: vercel-deploy
        continue-on-error: true
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
          working-directory: ./apps/frontend
      
      - name: Create GitHub Deployment (Vercel)
        if: github.ref == 'refs/heads/main'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            try {
              // Vérifier si le déploiement Vercel a réussi
              const vercelOutcome = '${{ steps.vercel-deploy.outcome }}';
              const vercelSuccess = vercelOutcome === 'success';
              const deployState = vercelSuccess ? 'success' : (vercelOutcome === 'failure' ? 'failure' : 'pending');
              
              console.log(`🔍 État du déploiement Vercel: ${vercelOutcome || 'not executed'}`);
              console.log(`📦 Création du GitHub Deployment avec statut: ${deployState}`);
              
              // Supprimer les anciens déploiements Production pour garder seulement le dernier
              const deployments = await github.rest.repos.listDeployments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                environment: 'Production',
                per_page: 100
              });
              
              console.log(`📦 ${deployments.data.length} ancien(s) déploiement(s) trouvé(s)`);
              
              // Supprimer tous les anciens déploiements (on garde seulement le nouveau)
              for (const deployment of deployments.data) {
                try {
                  await github.rest.repos.deleteDeployment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    deployment_id: deployment.id
                  });
                  console.log(`🗑️  Déploiement #${deployment.id} supprimé`);
                } catch (error) {
                  console.log(`⚠️  Erreur lors de la suppression du déploiement #${deployment.id}: ${error.message}`);
                }
              }
              
              // Créer le nouveau déploiement (le seul visible)
              console.log('✨ Création du nouveau déploiement Production...');
              const newDeployment = await github.rest.repos.createDeployment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: context.sha,
                environment: 'Production',
                auto_merge: false,
                required_contexts: [],
                description: `Deployed via GitHub Actions - ${context.sha.substring(0, 7)}`
              });
              
              console.log(`✅ Déploiement #${newDeployment.data.id} créé avec succès`);
              
              await github.rest.repos.createDeploymentStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                deployment_id: newDeployment.data.id,
                state: deployState,
                environment_url: 'https://www.veyl.io',
                description: vercelSuccess ? 'Deployment successful' : (vercelOutcome === 'failure' ? 'Deployment failed' : 'Deployment in progress')
              });
              
              console.log(`🎉 Statut de déploiement mis à jour: ${deployState}`);
            } catch (error) {
              console.error('❌ Erreur lors de la création du GitHub Deployment:', error);
              console.error('Détails:', JSON.stringify(error, null, 2));
              throw error;
            }

  backend:
    runs-on: ubuntu-latest
    needs: check-changes
    if: needs.check-changes.steps.filter.outputs.backend == 'true' || github.event_name == 'workflow_dispatch'
    outputs:
      railway-deploy-outcome: ${{ steps.railway-deploy.outcome }}
    defaults:
      run:
        working-directory: ./apps/backend
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: pip install -r requirements.txt
        
      - name: Run tests (if available)
        run: |
          if [ -f "test_*.py" ]; then
            python -m pytest
          else
            echo "No tests found, skipping..."
          fi
          
      - name: Deploy to Railway (production only)
        if: github.ref == 'refs/heads/main'
        id: railway-deploy
        continue-on-error: true
        run: |
          npx -y @railway/cli up --service insidr-production --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      
      - name: Create GitHub Deployment (Railway)
        if: github.ref == 'refs/heads/main'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            try {
              const envName = 'insidr (satisfied-mindfulness / production)';
              const railwayOutcome = '${{ steps.railway-deploy.outcome }}';
              const railwaySuccess = railwayOutcome === 'success';
              const deployState = railwaySuccess ? 'success' : (railwayOutcome === 'failure' ? 'failure' : 'pending');
              
              console.log(`🔍 État du déploiement Railway: ${railwayOutcome || 'not executed'}`);
              console.log(`📦 Création du GitHub Deployment avec statut: ${deployState}`);
              console.log(`🔍 Recherche des anciens déploiements Railway (${envName})...`);
              
              // Supprimer les anciens déploiements Railway pour garder seulement le dernier
              const deployments = await github.rest.repos.listDeployments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                environment: envName,
                per_page: 100
              });
              
              console.log(`📦 ${deployments.data.length} ancien(s) déploiement(s) trouvé(s)`);
              
              // Supprimer tous les anciens déploiements (on garde seulement le nouveau)
              for (const deployment of deployments.data) {
                try {
                  await github.rest.repos.deleteDeployment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    deployment_id: deployment.id
                  });
                  console.log(`🗑️  Déploiement #${deployment.id} supprimé`);
                } catch (error) {
                  console.log(`⚠️  Erreur lors de la suppression du déploiement #${deployment.id}: ${error.message}`);
                }
              }
              
              // Créer le nouveau déploiement (le seul visible)
              console.log(`✨ Création du nouveau déploiement Railway (${envName})...`);
              const newDeployment = await github.rest.repos.createDeployment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: context.sha,
                environment: envName,
                auto_merge: false,
                required_contexts: [],
                description: `Deployed via GitHub Actions - ${context.sha.substring(0, 7)}`
              });
              
              console.log(`✅ Déploiement #${newDeployment.data.id} créé avec succès`);
              
              await github.rest.repos.createDeploymentStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                deployment_id: newDeployment.data.id,
                state: deployState,
                environment_url: 'https://insidr-production.up.railway.app',
                description: railwaySuccess ? 'Deployment successful' : (railwayOutcome === 'failure' ? 'Deployment failed' : 'Deployment in progress')
              });
              
              console.log(`🎉 Statut de déploiement mis à jour: ${deployState}`);
            } catch (error) {
              console.error('❌ Erreur lors de la création du GitHub Deployment:', error);
              console.error('Détails:', JSON.stringify(error, null, 2));
              throw error;
            }

  # Job qui s'exécute toujours pour créer les GitHub Deployments
  # Même si les jobs frontend/backend ne s'exécutent pas (pour faire apparaître la section)
  # Test: vérifier que les anciens déploiements sont bien supprimés (devrait rester à 2 max)
  ensure-deployments-visible:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Create GitHub Deployment (Vercel)
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            try {
              console.log('🔍 Création du GitHub Deployment Production...');
              
              // Supprimer les anciens déploiements
              const deployments = await github.rest.repos.listDeployments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                environment: 'Production',
                per_page: 100
              });
              
              console.log(`📦 ${deployments.data.length} ancien(s) déploiement(s) trouvé(s)`);
              
              for (const deployment of deployments.data) {
                try {
                  await github.rest.repos.deleteDeployment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    deployment_id: deployment.id
                  });
                  console.log(`🗑️  Déploiement #${deployment.id} supprimé`);
                } catch (error) {
                  console.log(`⚠️  Erreur: ${error.message}`);
                }
              }
              
              // Créer le nouveau déploiement
              const newDeployment = await github.rest.repos.createDeployment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: context.sha,
                environment: 'Production',
                auto_merge: false,
                required_contexts: [],
                description: `Deployed via GitHub Actions - ${context.sha.substring(0, 7)}`
              });
              
              console.log(`✅ Déploiement #${newDeployment.data.id} créé`);
              
              await github.rest.repos.createDeploymentStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                deployment_id: newDeployment.data.id,
                state: 'success',
                environment_url: 'https://www.veyl.io'
              });
              
              console.log('🎉 Déploiement Production créé et visible');
            } catch (error) {
              console.error('❌ Erreur:', error.message);
              throw error;
            }
      
      - name: Create GitHub Deployment (Railway)
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            try {
              const envName = 'insidr (satisfied-mindfulness / production)';
              console.log(`🔍 Création du GitHub Deployment Railway (${envName})...`);
              
              const deployments = await github.rest.repos.listDeployments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                environment: envName,
                per_page: 100
              });
              
              console.log(`📦 ${deployments.data.length} ancien(s) déploiement(s) trouvé(s)`);
              
              for (const deployment of deployments.data) {
                try {
                  await github.rest.repos.deleteDeployment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    deployment_id: deployment.id
                  });
                  console.log(`🗑️  Déploiement #${deployment.id} supprimé`);
                } catch (error) {
                  console.log(`⚠️  Erreur: ${error.message}`);
                }
              }
              
              const newDeployment = await github.rest.repos.createDeployment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: context.sha,
                environment: envName,
                auto_merge: false,
                required_contexts: [],
                description: `Deployed via GitHub Actions - ${context.sha.substring(0, 7)}`
              });
              
              console.log(`✅ Déploiement #${newDeployment.data.id} créé`);
              
              await github.rest.repos.createDeploymentStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                deployment_id: newDeployment.data.id,
                state: 'success',
                environment_url: 'https://insidr-production.up.railway.app'
              });
              
              console.log('🎉 Déploiement Railway créé et visible');
            } catch (error) {
              console.error('❌ Erreur:', error.message);
              throw error;
            }
